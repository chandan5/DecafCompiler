class Program {
    int a, b, c, d[20];
    int d[100], e[1000], f[0x6affdf];
    boolean xxx;
    boolean x[12];
    int someFunc(int y, boolean xxx) {
        int a, b;
        if(e != a) {
            callout("scanf",2,a,'c');
        }
        for i=2+3 , 10 {
            x = y+i;
            y = 2+3*(7);
        }
        func1(a,'a');
        return (12+(12-3));
    }
    void main() {
    }
}
